[pytest]
# When running pytest from the `sow-backend` folder, `tests` contains the tests
testpaths = tests
# Ensure `src` (the package root) is on PYTHONPATH so `import src.app` works.
pythonpath = src
